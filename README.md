# Token Launchpad

A React-based Solana token creation interface. Create SPL tokens with custom name, symbol, image, and supply using Solana wallet integration.

## Features
- Connect Solana wallet
- Create SPL tokens on Solana blockchain
- Customizable token metadata
- Built with React + Vite + Tailwind CSS

## Quick Start
```bash
npm install
npm run dev
```